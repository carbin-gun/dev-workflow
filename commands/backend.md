---
description: "Backend development with Go + Gin + gRPC + DDD architecture"
disable-model-invocation: true
---

Use the Skill tool to invoke `dev-workflow:developing-backend` and follow it exactly as presented to you
